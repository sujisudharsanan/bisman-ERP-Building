name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  node-matrix:
    name: CI — frontend & backend
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: frontend
            path: my-frontend
          - name: backend
            path: my-backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Cache npm per project
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ matrix.path }}-${{ hashFiles(format('{0}/package-lock.json', matrix.path)) }}
          restore-keys: |
            ${{ runner.os }}-node-${{ matrix.path }}-

      - name: Run install/test/build if package.json exists
        working-directory: ${{ matrix.path }}
        run: |
          echo "Running CI for ${{ matrix.name }} (path=${{ matrix.path }})"
          if [ -f "package.json" ]; then
            if [ -f "package-lock.json" ]; then
              echo "package-lock.json found — using npm ci"
              npm ci --no-progress
            else
              echo "No package-lock.json — falling back to npm install"
              npm install --no-progress
            fi

            if npm test --silent; then
              echo "npm test succeeded"
            else
              echo "npm test failed or no tests defined — attempting build if available"
              npm run build --if-present || echo "no build or build failed"
            fi
          else
            echo "No package.json in ${{ matrix.path }} — skipping Node steps"
          fi
