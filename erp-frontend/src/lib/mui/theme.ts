import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: { primary: { main: '#0ea5e9' }, secondary: { main: '#06b6d4' } },
  typography: { fontFamily: 'Inter, Roboto, sans-serif' }
})

export default theme
