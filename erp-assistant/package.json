{
  "name": "erp-assistant",
  "version": "1.0.0",
  "description": "Internal ERP assistant bot for Mattermost",
  "scripts": {
    "build": "webpack --mode production",
    "build:server": "cd server && go build -o dist/plugin-darwin-amd64",
    "package": "npm run build && npm run build:server && tar -czf erp-assistant-complete.tar.gz server/dist/ webapp/dist/ plugin.json README.md"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "babel-loader": "^9.1.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
