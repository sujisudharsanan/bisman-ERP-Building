# Keep Docker context tiny to avoid timeouts in Railway
# Include only backend for the root Dockerfile

# Default: ignore everything
**

# Allow root Dockerfile and only my-frontend when building frontend image
!Dockerfile
!my-frontend/**

# Exclude heavy/unnecessary items even under my-backend
my-backend/node_modules
my-backend/logs
my-backend/**/*.log
my-backend/**/*.dump
my-backend/**/*.sqlite
my-backend/*.sqlite*
my-backend/.env*
my-backend/coverage
my-backend/__tests__

# Root noise and large dirs
!.dockerignore
!.gitignore
.git
node_modules
.next
coverage
logs
**/*.log
**/*.dump
.env*

# Frontend heavies excluded even though directory is included
my-frontend/node_modules
my-frontend/.next
my-frontend/out
my-frontend/coverage
my-frontend/.env*

# OS/editor
.DS_Store
.vscode
.idea
