This page was removed from the sidebar and the route is deprecated.

If you need to restore it:
- Re-add the `common-notifications` entry in `src/common/config/page-registry.ts`.
- Ensure the route `/common/notifications` remains accessible if linked directly.
