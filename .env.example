# Local development environment example
# Do NOT commit real secrets. Copy to .env and fill values.

DB_USER=erp_admin
DB_PASSWORD=Suji@123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_main

# Optional: set DRY_RUN=true to run the app in dry-run mode
DRY_RUN=false

# Example environment variables for local dev
PORT=3000
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
SENTRY_DSN=
JAEGER_ENDPOINT=
SERVICE_NAME=erp-service
NODE_ENV=development
