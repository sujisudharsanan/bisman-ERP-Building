# Chat Module Architecture Diagram 🏗️

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         BISMAN ERP - Chat Module                        │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                              FRONTEND                                   │
│                    (my-frontend/src/modules/chat/)                      │
└─────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────┐
    │                    Next.js Routes                        │
    │  /chat (Messages) | /chat/ai (AI Assistant)             │
    └────────────────────────┬─────────────────────────────────┘
                             │
    ┌────────────────────────▼─────────────────────────────────┐
    │                     Components                           │
    │  • ChatInterface    (Main chat UI)                       │
    │  • ChatGuard        (Visibility guard + floating widget) │
    │  • FloatingWidget   (Chat button)                        │
    │  • AIWidget         (AI assistant widget)                │
    │  • CallControls     (Jitsi call controls)                │
    └────────────┬───────────────────────────┬─────────────────┘
                 │                           │
    ┌────────────▼──────────┐   ┌────────────▼─────────────┐
    │        Hooks          │   │        Services          │
    │  • useChat()          │   │  • chatApi               │
    │  • useSocket()        │   │    - getThreads()        │
    │    - threads          │   │    - sendMessage()       │
    │    - messages         │   │    - initiateCall()      │
    │    - sendMessage()    │   │    - sendAIMessage()     │
    └───────────────────────┘   └──────────────────────────┘
                 │                           │
                 └───────────┬───────────────┘
                             │
                    ┌────────▼────────┐
                    │      Types      │
                    │  Thread         │
                    │  Message        │
                    │  User           │
                    │  CallLog        │
                    └─────────────────┘

═══════════════════════════════════════════════════════════════════════════
                              HTTP / WebSocket
═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                              BACKEND                                    │
│                    (my-backend/modules/chat/)                           │
└─────────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────┐
    │                    Express Routes                        │
    │              (Base: /api/chat)                           │
    └────────────────────────┬─────────────────────────────────┘
                             │
    ┌────────────────────────▼─────────────────────────────────┐
    │               modules/chat/routes/                       │
    │                                                          │
    │  ┌────────────┐  ┌────────────┐  ┌────────────┐        │
    │  │   ai.js    │  │messages.js │  │  calls.js  │        │
    │  │ (AI Chat)  │  │ (Threads)  │  │  (Jitsi)   │        │
    │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘        │
    │        │               │               │                │
    │        └───────────────┼───────────────┘                │
    │                        │                                │
    │                 ┌──────▼──────┐                         │
    │                 │  index.js   │                         │
    │                 │ (Aggregator)│                         │
    │                 └─────────────┘                         │
    └──────────────────────────────────────────────────────────┘
                             │
    ┌────────────────────────▼─────────────────────────────────┐
    │               Socket.IO Namespace                        │
    │                    /chat                                 │
    │  ┌──────────────────────────────────────────────┐       │
    │  │  chatSocket.js (JWT Auth)                    │       │
    │  │  Events:                                     │       │
    │  │  • chat:join      • chat:message             │       │
    │  │  • chat:leave     • chat:typing              │       │
    │  │  • chat:read      • chat:presence            │       │
    │  └──────────────────────────────────────────────┘       │
    └──────────────────────────────────────────────────────────┘
                             │
    ┌────────────────────────▼─────────────────────────────────┐
    │                     Services                             │
    │  ┌─────────────────┐  ┌──────────────────┐              │
    │  │ unifiedChatEngine│  │  rbacService     │              │
    │  │ (AI Logic)       │  │  (Permissions)   │              │
    │  └─────────────────┘  └──────────────────┘              │
    └──────────────────────────────────────────────────────────┘
                             │
    ┌────────────────────────▼─────────────────────────────────┐
    │                   Database (Prisma)                      │
    │  Tables:                                                 │
    │  • threads          • messages          • call_logs      │
    │  • thread_members   • message_reactions                  │
    └──────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
                           External Services
═══════════════════════════════════════════════════════════════════════════

    ┌──────────────┐         ┌──────────────┐
    │    Jitsi     │         │    Ollama    │
    │  Video/Audio │         │  AI Models   │
    │  Port: 8443  │         │  Port: 11434 │
    └──────────────┘         └──────────────┘


═══════════════════════════════════════════════════════════════════════════
                              Data Flow
═══════════════════════════════════════════════════════════════════════════

   User Action (Send Message)
          │
          ▼
   ChatInterface Component
          │
          ▼
   useChat Hook
          │
          ▼
   chatApi.sendMessage()
          │
          ▼
   HTTP POST /api/chat/threads/:id/messages
          │
          ▼
   Backend: messages.js Router
          │
          ├──► Validate with rbacService
          │
          ├──► Save to Database (Prisma)
          │
          └──► Emit Socket.IO Event
                    │
                    ▼
          All Connected Clients Receive Update
                    │
                    ▼
          useSocket Hook Receives Event
                    │
                    ▼
          Update Component State
                    │
                    ▼
          UI Updates Automatically


═══════════════════════════════════════════════════════════════════════════
                         Authentication Flow
═══════════════════════════════════════════════════════════════════════════

   User Visits /chat
          │
          ▼
   ChatGuard Component
          │
          ├──► Check isAuthenticated (AuthContext)
          │
          ├──► Check if page is public
          │
          └──► Render FloatingWidget or ChatInterface
                    │
                    ▼
          User Opens Chat
                    │
                    ▼
          Socket.IO Connection Established
                    │
          ├──► Send JWT Token in Handshake
          │
          ├──► Backend Validates Token (jwtMiddleware)
          │
          └──► Connection Accepted or Rejected


═══════════════════════════════════════════════════════════════════════════
                              File Structure
═══════════════════════════════════════════════════════════════════════════

FRONTEND
my-frontend/src/
├── modules/chat/
│   ├── components/          # React UI components
│   │   ├── ChatInterface.tsx
│   │   ├── ChatGuard.tsx
│   │   ├── FloatingWidget.tsx
│   │   ├── AIWidget.tsx
│   │   └── CallControls.tsx
│   ├── hooks/               # Custom React hooks
│   │   ├── useChat.ts
│   │   └── useSocket.ts
│   ├── services/            # API client
│   │   └── chatApi.ts
│   ├── pages/               # Page components
│   │   ├── index.tsx
│   │   └── ai-assistant.tsx
│   ├── types/               # TypeScript definitions
│   │   └── index.ts
│   └── index.ts             # Module exports
│
├── app/
│   └── chat/
│       ├── page.tsx         # /chat route
│       └── ai/
│           └── page.tsx     # /chat/ai route
│
└── config/
    └── roleLayoutConfig.ts  # Navigation config (updated)


BACKEND
my-backend/
├── modules/chat/
│   ├── routes/
│   │   ├── index.js         # Main router
│   │   ├── ai.js            # AI endpoints
│   │   ├── messages.js      # Message endpoints
│   │   └── calls.js         # Call endpoints
│   ├── socket/
│   │   └── chatSocket.js    # Socket.IO handlers
│   └── README.md
│
├── app.js                   # Mount /api/chat (updated)
└── server.js                # Initialize chat socket (updated)


═══════════════════════════════════════════════════════════════════════════
                           Integration Points
═══════════════════════════════════════════════════════════════════════════

1. AuthContext (Frontend)
   └─► Provides user authentication state to ChatGuard

2. Prisma (Backend)
   └─► Database ORM for threads, messages, call logs

3. Socket.IO (Both)
   └─► Real-time bidirectional communication

4. RBAC Service (Backend)
   └─► Role-based access control for chat features

5. Jitsi Meet (External)
   └─► Video/audio calling infrastructure

6. Ollama (External)
   └─► AI model inference for intelligent responses


═══════════════════════════════════════════════════════════════════════════
                             Key Features
═══════════════════════════════════════════════════════════════════════════

✅ Modular Architecture        ✅ Real-time Updates
✅ TypeScript Support          ✅ JWT Authentication
✅ Socket.IO Namespace         ✅ RBAC Integration
✅ AI Assistant                ✅ Video/Audio Calls
✅ Thread Management           ✅ Typing Indicators
✅ Read Receipts               ✅ User Presence
✅ Message Reactions           ✅ File Attachments
✅ Search Functionality        ✅ Role-based Access


═══════════════════════════════════════════════════════════════════════════
                            Health Checks
═══════════════════════════════════════════════════════════════════════════

Backend Health:
  curl http://localhost:5000/api/chat/health
  
  Response:
  {
    "module": "chat",
    "status": "ok",
    "features": {
      "ai": true,
      "threads": true,
      "calls": true,
      "realtime": true
    }
  }

Socket.IO Status:
  Browser Console:
  [Socket.IO] Connected: <socket-id>
  [Chat] ✅ Socket.IO chat handlers initialized


═══════════════════════════════════════════════════════════════════════════
                          Version Information
═══════════════════════════════════════════════════════════════════════════

Migration Date:     November 27, 2025
Frontend Framework: Next.js 15.5.6
Backend Framework:  Express.js
Real-time:          Socket.IO
Database:           PostgreSQL (Prisma ORM)
AI Integration:     Ollama (port 11434)
Video Calls:        Jitsi Meet
```

## 🎯 Navigation Flow

```
User Logs In
    │
    ▼
Role-based Dashboard
    │
    ├─► MANAGER Dashboard
    │   └─► Sidebar Menu
    │       ├─► Messages (/chat)
    │       └─► AI Assistant (/chat/ai)
    │
    └─► STAFF Dashboard
        └─► Sidebar Menu
            ├─► Messages (/chat)
            └─► AI Assistant (/chat/ai)
```

## 🔄 Message Lifecycle

```
1. User types message
      ↓
2. ChatInterface captures input
      ↓
3. useChat hook calls sendMessage()
      ↓
4. chatApi.sendMessage() sends HTTP POST
      ↓
5. Backend validates & saves to DB
      ↓
6. Backend emits Socket.IO event
      ↓
7. All clients receive real-time update
      ↓
8. useSocket hook updates state
      ↓
9. UI re-renders with new message
```

## 📊 Database Schema

```sql
-- Threads Table
CREATE TABLE threads (
  id UUID PRIMARY KEY,
  title VARCHAR(255),
  type VARCHAR(50),
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);

-- Messages Table
CREATE TABLE messages (
  id UUID PRIMARY KEY,
  thread_id UUID REFERENCES threads(id),
  user_id UUID,
  content TEXT,
  type VARCHAR(50),
  created_at TIMESTAMP,
  edited_at TIMESTAMP
);

-- Call Logs Table
CREATE TABLE call_logs (
  id UUID PRIMARY KEY,
  thread_id UUID REFERENCES threads(id),
  initiated_by UUID,
  call_type VARCHAR(20),
  started_at TIMESTAMP,
  ended_at TIMESTAMP,
  duration INTEGER
);

-- Thread Members Table
CREATE TABLE thread_members (
  thread_id UUID REFERENCES threads(id),
  user_id UUID,
  role VARCHAR(50),
  joined_at TIMESTAMP,
  PRIMARY KEY (thread_id, user_id)
);
```

---

**Architecture Documentation Complete** 🎊  
For implementation details, see `CHAT_MODULE_QUICK_REFERENCE.md`
